BST查找和折半查找的时间复杂度都是O(logn),但是如果序列有规律，那么建立的树就会导致BST不能够满足平衡性。当使用特定的数据进行查询的时候可能会导致性能下降，比如本次实验采用数据一时，建立的树就会形成一条链，也就是右斜树，导致每次查找后面的数据性能特别差，对于随机的数进行查找效果仍然很差。但是如果序列不是规律的，而是随机排列的，比如数据二，我们发现它的平均查找时间性能和折半查找时间性能很接近。因此，当数字序列分布随机时，折半查找和BST查找的时间性能差不多。
